import React, { Fragment } from "react";
import { Text } from "react-native";
export const ScreenTestStack = ({navigation}) => {

    return (
        <Fragment>
            <Text>it`s Screen stack demo</Text>
        </Fragment>
    );
}